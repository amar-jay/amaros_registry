1. proper AI configuration of package installation 
- if ?download. package and send everything as zip
- if /<name>/readme.md. download readme 
- if /<name>/readme.md. download readme *
- if /<name>/metadata.json download metadata *
- if /<name>/versions/<version>.tar.gz download that version
- if /<name>/versions/latest download latest version
- if /<name>/versions return list of versions available
- if /node?q= search for a node and return info about it
- if /node return the list of data(latest version, number of versions, author,...) on each node.
